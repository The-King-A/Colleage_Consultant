# RAG 知识库 PDF

本目录下的 PDF 会在后端启动时由 `CommonConfig` 加载并向量化（LangChain4j + Redis）。

## 大文件说明

`2023河南理科招生之友.pdf`（约 220MB）超过 GitHub 单文件 100MB 限制，**未纳入仓库**。

克隆项目后请自行将该校验资料放入本目录（文件名需一致），再启动 Java 后端以完成 RAG 入库。

其余体积较小的院校/专业说明 PDF 已随仓库提供。
